# AI Smart Farmer Assistant – Python Requirements
# Install with: pip install -r requirements.txt

flask==3.0.3
flask-cors==4.0.1
Pillow==10.3.0
numpy==1.26.4
mysql-connector-python==8.4.0

# TensorFlow – choose ONE line based on your hardware:
tensorflow==2.16.1            # CPU version (works everywhere)
# tensorflow-gpu==2.12.0      # GPU version (uncomment if you have NVIDIA GPU)

# Optional: OpenCV (for advanced image pre-processing)
opencv-python-headless==4.9.0.80
